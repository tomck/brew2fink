[project]
name = "brew2fink"
version = "0.1.1"
requires-python = ">=3.9"
dependencies = ["macpkg-migrate>=0.3,<0.4"]
[project.scripts]
brew2fink = "brew2fink.cli:main"
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
